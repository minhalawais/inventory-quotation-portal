MONGODB_URI=mongodb://127.0.0.1:27017/inventory_portal
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=replace-with-a-long-random-secret

# Optional, only if configured in your app.
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
